//=====================================================
// Author      : ahmedgbr367
// Date        : 2026-01-06 12:38:11
// Problem Name: A_Little_Fairy_s_Painting
// Note        : 
//=====================================================
#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    cin.tie(0)->sync_with_stdio(0);
    int t; cin >> t;
    while(t--){
        int n;cin >> n;
        set<int>st;
        int arr[n];
        for(int i = 0 ;i < n;i++){
            cin >>arr[i];
            st.insert(arr[i]);            
        }
        int habd = n - st.size();
        cout << arr[n-habd] << '\n';

    }
    return 0;
}
